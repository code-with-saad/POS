# MongoDB connection string (optional — defaults to mongodb://127.0.0.1:27017/pos if unset)
MONGODB_URI=mongodb://127.0.0.1:27017/pos

# Server port
PORT=5000

# Frontend origin for CORS (Vite default)
CLIENT_ORIGIN=http://localhost:5173
